# 🔐 ENVIRONMENT VARIABLES

# Odoo ERP Configuration
ODOO_URL=https://erp.zuhalmuzik.com
ODOO_DATABASE=erp.zuhalmuzik.com
ODOO_USERNAME=user@example.com
ODOO_API_KEY=your-api-key-here

# JWT Secret (üretmek için: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Node Environment
NODE_ENV=development

# ======================================
# KULLANIM TALİMATLARI
# ======================================

# 1. Bu dosyayı kopyala:
#    cp .env.example .env

# 2. .env dosyasını düzenle:
#    nano .env  (veya code .env)

# 3. Gerçek değerleri gir:
#    - ODOO_API_KEY: Odoo ERP'den API key al
#    - JWT_SECRET: Yeni secret oluştur (openssl rand -base64 32)

# 4. .gitignore'da olduğundan emin ol:
#    echo ".env" >> .gitignore

# 5. Vercel'de ayarla:
#    Vercel Dashboard → Settings → Environment Variables
#    Bu değerleri ekle

# 6. GitHub Actions'da ayarla:
#    GitHub → Settings → Secrets and variables → Actions
#    Bu değerleri ekle

# ⚠️ DİKKAT:
# - .env dosyasını ASLA git'e ekleme!
# - API key'leri kimseyle paylaşma
# - Production'da güçlü JWT_SECRET kullan
